# Frontend for nono_rent

Le frontend charge les web components via `static/dist/index.js`.

Dans les images Docker, ce dossier est construit depuis le submodule `design-system`
au moment du build (voir `Dockerfile.frontend`).
